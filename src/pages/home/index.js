import { Text, Box } from "goods-core";
import { Button } from "goods-ui";
import React, { lazy, Suspense, useEffect } from "react";

function HomePage({ openAction }) {
  return (
    <Box>
      <Text>Hello</Text>
      <Button>haha</Button>
    </Box>
  );
}

export default HomePage;
